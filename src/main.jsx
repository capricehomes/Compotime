import React from "react";
import ReactDOM from "react-dom/client";
import WAComputationOfTimeApp from "./WAComputationOfTimeApp";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <WAComputationOfTimeApp />
  </React.StrictMode>
);
